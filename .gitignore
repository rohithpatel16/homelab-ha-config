# --- Secrets and credentials: NEVER commit --
secrets.yaml
.storage/
*.key
*.pem
.google.token
ip_bans.yaml

# Zigbee2MQTT: device/pairing layer (snapshot-recovered, not git) AND it embeds
# the Zigbee network key + MQTT credentials. Whole dir is out of bounds.
zigbee2mqtt/

# --- Runtime state and databases (transient, huge) ---
home-assistant_v2.db
home-assistant_v2.db-*
*.db
*.db-shm
*.db-wal
*.log
*.log.*

# --- Caches / generated ---
__pycache__/
deps/
tts/
.cloud/
.uuid
.HA_VERSION
.ha_run.lock
.cache/
backups/

# --- OS Noise ---
.DS_Store